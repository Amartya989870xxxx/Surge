scenario_id: marketing_traffic_01
title: Low-intent campaign traffic dilutes conversion
category: diagnosis
description: A paid campaign nearly doubles sessions with low-intent visitors; checkout itself is healthy.
request: "Our conversion rate dropped yesterday. Investigate why and coordinate the response."
world: marketing_traffic
ground_truth:
  expected_outcome: marketing_traffic_quality
  required_evidence:
    - {key: conversion_drop, app: sheets, source_type: metric_anomaly}
    - {key: paid_traffic_shift, app: sheets, source_type: traffic_mix}
    - {key: campaign_announcement, app: slack, source_type: message, content_contains: "creator campaign"}
  relevant_evidence:
    - {app: sheets, source_type: funnel_analysis}
    - {app: sheets, source_type: tracking_consistency}
    - {app: sheets, source_type: error_rate_analysis}
    - {app: github, source_type: deployment_absence}
    - {app: slack, source_type: message}
  expected_action: create_github_issue
  approval_required: true
  expect_verified: true
